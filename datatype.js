"use strict"; // it is used to tell code based on modern js.

let fname="ved"; // string
let age=89; // number
let isLogged=true; // boolean
let temp=null; //object(null)
let address; // undefined
let stockItem=BigInt(45); // bigint

console.table([fname,address,age,isLogged,temp,stockItem])

console.log(typeof(fname));
console.log(typeof stockItem);
console.log(typeof(temp));